spring:
  datasource:
    url: jdbc:mysql://localhost:3306/assignment
    username: root
    password: suman
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  error:
    include-message: always